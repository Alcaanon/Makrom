import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Makrom-Cliente',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
